#include <bits/stdc++.h>
using namespace std;

int main() {
  freopen("/dev/random", "r", stdin);
  freopen("garbage", "w", stdout);
  while (true) {
    int a;
    cin >> a;
    cerr << a << ' ';
  }
  return 0;
}
